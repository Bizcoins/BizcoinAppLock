name: Android CI

# Triggers the workflow only manually via workflow_dispatch.
on:
  # The push and pull_request events are REMOVED.
  workflow_dispatch:
    # Optional: You can add inputs here if you want to pass parameters
    # inputs:
    #   environment:
    #     description: 'Environment to deploy to'
    #     required: true
    #     default: 'Test'

jobs:
  build:
    runs-on: ubuntu-latest # Use the latest Ubuntu runner

    steps:
      - name: Checkout repository
        # ... steps remain the same ...
        uses: actions/checkout@v4.2.2

      # ... remaining steps for setup, build, and upload ...
- name: Run Gemini CLI
  uses: google-github-actions/run-gemini-cli@v0.1.15
