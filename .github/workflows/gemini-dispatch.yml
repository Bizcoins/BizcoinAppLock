      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Needed for full context

          - uses: google/gemini-action@v1 # <-- CHANGE IS HERE
        with:
          gemini_api_key: ${{ secrets.GEMINI_API_KEY }}